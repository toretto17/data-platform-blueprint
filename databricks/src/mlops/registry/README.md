# registry

Register a model version in UC, promote between stages (None → Champion),  and retrieve the latest production model URI.

## Files

- `registry.py`

## Platform twin

`./aws/src/mlops/registry/`
