# marts

Template for Gold-layer ETL jobs (Silver → Gold).  Handles: multi-source joins, aggregations, window functions,  period aggregates (MTD/YTD/WTD), zero-fill, comparison periods.

## Files

- `gold_job.py`

## Platform twin

`./databricks/src/gold/marts/`
