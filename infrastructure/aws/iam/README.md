# 📁 IAM example policies

Reference JSON policies for Glue + SageMaker roles. Use these as a starting point
for your Terraform IAM module or manual console setup.
- `glue_role_policy.json` — S3, Glue, CloudWatch, LakeFormation
- `sagemaker_role_policy.json` — SageMaker, S3, iam:PassRole
